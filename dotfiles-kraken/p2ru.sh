if [ -d /etc/profile.d ]; then
    for i in /etc/profile.d/custom.sh; do
    if [ -r  ]; then
            . 
        fi
    done
    unset i
fi
if [ -d /etc/profile.d ]; then
    for i in /etc/profile.d/custom.sh; do
    if [ -r  ]; then
            . 
        fi
    done
    unset i
fi
